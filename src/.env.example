# Copy to src/.env (gitignored) — never commit real keys.
#   cp src/.env.example src/.env
#
# Used by verify:llm-live, Maestro ML demos, and optional in-app LLM paste.
# Keys are read at runtime from this file or process env (OPENAI_API_KEY, etc.).

OPENAI_API_KEY=
OPENROUTER_API_KEY=

# Optional — enable when you have keys (automation skips if unset)
GROQ_API_KEY=
GEMINI_API_KEY=

# Optional — force provider for Maestro F18: openai | openrouter | groq | gemini
# DEMO_LLM_PROVIDER=openrouter

# Optional — override OpenAI-compatible endpoint/model for verify:llm-live
# LLM_BASE_URL=
# LLM_MODEL=

# Appetize / BrowserStack upload (optional — never commit real values)
APPETIZE_API_TOKEN=
APPETIZE_PUBLIC_KEY_ANDROID=
APPETIZE_PUBLIC_KEY_IOS=
BROWSERSTACK_USERNAME=
BROWSERSTACK_ACCESS_KEY=
