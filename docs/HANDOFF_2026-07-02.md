# Handoff Status — 2026-07-02

This document is the handoff for the next agent to continue from the current repository state after the post-merge hardening pass.

## Branches

- Stable hardening branch used for this pass: `codex/post-merge-hardening`
- Historical hybrid-search branch: `codex/hybrid-search-v1`
- Local `main` before this handoff was at `3194b4e` (`stale-session-on-startup`)

## What Is Completed

### 1. Auth/session hardening

Completed:

- startup session restoration validation
- forced logout path for invalid/expired protected-session responses
- shared auth-failure interceptor in `src/services/apiClient.js`
- bootstrap wiring in `App.jsx`
- thunk updates in `src/redux/authSlice.js`
- tests for auth/session behavior

Files:

- `App.jsx`
- `src/services/apiClient.js`
- `src/redux/authSlice.js`
- `__tests__/apiClient.test.js`
- `__tests__/searchRuntimeClient.test.js`

### 2. iOS checkout/cart hardening

Completed:

- checkout screen converted to keyboard-aware scroll layout
- iPhone flow no longer traps the user above the Place Order CTA after phone entry
- regression test added for keyboard-aware layout contract

Files:

- `src/screens/CheckoutScreen.jsx`
- `__tests__/CheckoutScreen.test.js`

### 3. iOS commerce simulator verification

Verified on booted `iPhone 17 Pro Max` (`iOS 26.5`):

- login
- product list
- PDP
- Add to Cart
- cart
- checkout
- order summary
- orders tab

Artifacts:

- refreshed `docs/demo/videos/app-flow-demo.mp4`
- Maestro app-flow updates in `.maestro/demo-app-flow.yaml`
- recorder improvements in `scripts/record-demo-ios.mjs`

### 4. Automated verification

Latest verified results during this pass:

- Jest: `83/83` tests passing across `25/25` suites
- Hybrid search verification: `20/20`
- Hybrid ML verification: `13/13`

Notes:

- `App.test.tsx` still emits the pre-existing React `act(...)` warning on unmount
- search/ML runtime checks passed against the live hybrid API on `http://127.0.0.1:5002`

### 5. Documentation brought to current truth

Updated:

- `README.md`
- `docs/ARCHITECTURE.md`
- `docs/DEPLOYMENT.md`
- `docs/HYBRID_SEARCH_TEST_STEPS.md`
- `docs/SETUP.md`
- `docs/TESTING_STATUS.md`

Corrected items:

- Jest counts now reflect `83/83` and `25/25`
- deployment/persistence docs now reflect local JSON persistence via `server/data/store.json`
- deployment doc now distinguishes API hosting from browser-based demo platforms

## What Is Not Fully Completed

### iOS ML Maestro demo flow

Status: not fully green yet.

Important:

- this does **not** currently indicate backend search failure
- it is currently a simulator automation/harness problem on the Home-screen LLM controls

Current evidence:

- backend hybrid verification is green
- iOS ML flow already proved product-search navigation on simulator
- remaining failure is at the Home-screen `AI reasoning (LLM)` toggle/provider selection path in `.maestro/demo-ml-features.yaml`

Observed issue:

- Maestro can reach Home and Products
- it can run the text-search part
- it still does not reliably toggle the LLM switch to the expanded provider-key state on iOS
- because of that, provider chip selection like `OpenAI`/`OpenRouter` never becomes available in the flow

Files involved:

- `.maestro/demo-ml-features.yaml`
- `scripts/record-demo-ios.mjs`
- `docs/demo/videos/ml-features-demo.mp4`

Recommendation:

- do **not** treat this as a blocker for the backend search framework itself
- do treat it as unfinished iOS demo automation work before claiming a fully green ML simulator demo

## Recommended Merge Scope

Safe to merge now:

- auth/session hardening
- checkout keyboard fix
- new regression tests
- documentation corrections
- iOS app-flow recorder fixes
- refreshed commerce demo recording

Hold or re-verify before merge if strict demo-completeness is required:

- `.maestro/demo-ml-features.yaml`
- `docs/demo/videos/ml-features-demo.mp4`

## Next Steps For The Next Agent

### Priority 1

Finish the iOS ML demo harness:

1. make the iOS Home-screen LLM toggle interaction deterministic
2. confirm provider chip visibility/selection after toggle
3. verify key entry into the provider input
4. complete the typed LLM search path
5. complete the photo-search path
6. refresh `docs/demo/videos/ml-features-demo.mp4`

### Priority 2

After the iOS ML harness is green:

1. rerun the iOS ML recording
2. rerun full Jest
3. rerun `npm run verify:search`
4. rerun `npm run verify:ml`
5. decide whether the ML Maestro artifacts should be committed on top of the current merge

### Priority 3

Deployment track:

1. deploy the API first
2. use persistent storage for catalog snapshot and prebuilt embeddings
3. only after that, create one public demo build
4. use Appetize for browser-shareable app demos
5. use BrowserStack App Live for broader QA/device review

## Hosting Recommendation Snapshot

For the current architecture:

- best near-term hosting model: public API first, mobile demo second
- preferred low-cost backend path: `Render`, `Railway`, or `Fly.io`
- strongest browser-shareable mobile demo platform: `Appetize`
- strongest post-merge QA platform for real devices: `BrowserStack App Live`

## Commands Recently Proven

```bash
npm test -- --runInBand --watchman=false
API_URL=http://127.0.0.1:5002 npm run verify:search
API_URL=http://127.0.0.1:5002 npm run verify:ml
RECORD_ONLY=app-flow npm run record:demo:ios
```

## Final Assessment

The codebase is in a materially better state than before this pass:

- auth/session behavior is safer
- iOS checkout is more robust
- docs reflect reality
- iOS commerce flow has direct simulator evidence
- hybrid search backend checks are green

The only meaningful unfinished item from this pass is the iOS ML Home-screen automation path, and that should be handled as a follow-up harness/UI-verification task rather than as a core search-framework failure.
